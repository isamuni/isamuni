<% if notice %>
<p id="notice" class="alert alert-info" role="alert">
  <%= notice %>
</p>
<% end %>

<h1>WikIsamuni!</h1>

<p>
  Benvenuto nel wiki interno di isamuni. Questo wiki consiste di pagine "libere",
  che non descrivono aziende o community.
</p>
<p>
  Per editare le pagine nel wiki devi avere i permessi di editor. Contattaci a
  vigliag@gmail.com o simone.ivan.conte@gmail.com e saremo felici di abilitare
  il tuo account.
</p>

<table class="table table-striped table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Slug</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @wikipages.each do |wikipage| %>
      <tr>
        <td><%= wikipage.name %></td>
        <td><%= wikipage.slug %></td>
        <td><%= link_to 'Show', wikipage %></td>
        <td><%= link_to 'Edit', edit_wikipage_path(wikipage) %></td>
        <td><%= link_to 'Destroy', wikipage, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuova pagina wiki', new_wikipage_path %>
